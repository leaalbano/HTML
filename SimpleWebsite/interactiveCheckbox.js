/* Checks if box is clicked & adds a check inside */
var ifCheckView;
if (test_checkBox.checked) {
    ifCheckView = "Checked";
}
else if (!test_checkBox.checked) {
    ifCheckView = "Not Checked";
}
